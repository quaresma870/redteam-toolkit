click>=8.1.0
rich>=13.7.0
pyyaml>=6.0.1
dnspython>=2.6.0
cryptography>=42.0.0
reportlab>=4.0.0
schedule>=1.2.0             # `schedule` subcommand (recon-only — see cli.py's schedule() docstring)

# Dev / test
pytest>=8.2.0
ruff>=0.4.0

# Dashboard tests (starlette.testclient.TestClient requires httpx2,
# not httpx, on current starlette versions -- same gap found and
# fixed in the sibling secureaudit repo; confirmed here by actually
# hitting the RuntimeError on a clean environment before fixing it)
fastapi>=0.110.0
httpx2>=0.27.0
